// Tipos compartilhados de UI.

// Arquivo gerado/derivado, usado no preview (FilePreview) e no export.
export interface GeneratedFile {
  name: string;
  path: string;
  content: string;
  language: string;
}
